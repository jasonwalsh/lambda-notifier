jobs:
  test:
    docker:
      - image: python:3.7
    steps:
      - checkout
      - run: pip install pytest
      - run: python -m pytest tests
version: 2.0
workflows:
  test:
    jobs:
      - test
  version: 2
