jobs:
  test:
    docker:
      - image: python:3.7
    steps:
      - checkout
      - run: pip install pytest pytest-cov pytest-flake8
      - run: python -m pytest
version: 2.0
workflows:
  test:
    jobs:
      - test
  version: 2
